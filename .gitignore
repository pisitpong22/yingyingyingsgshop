# Firebase
.firebase/
*.log

# Dependencies
node_modules/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
.claude/settings.local.json

# Secrets (never commit these)
*-firebase-adminsdk-*.json
serviceAccountKey.json
.env
.env.local

# Local design scratch — 20 MB of layout prototypes and a 19 MB .glb.
# Not deployed (firebase.json ignores dot-directories via **/.*/**), but it
# showed up as untracked in every `git status`.
.preview/
